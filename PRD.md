1. 核心交互模式：项目看板 (Project Kanban)
- 展现形式：每个任务作为一个独立的卡片或列，横向排列。
- 内容：卡片内垂直列出该任务的所有拆解步骤（Checklist）。
- 操作：
  - 直接在看板中勾选步骤。
  - 点击步骤名称可直接编辑文字。
  - 提供“添加步骤”按钮和“删除”图标。
2. 优化后的工作流 (Refined Workflow)
1. 创建阶段：用户输入任务描述 + DDL。
2. AI 介入：AI 将描述转化为一系列初始步骤。
3. 项目激活：任务进入看板。此时用户拥有完全控制权：
  - 改名：修改 AI 生成的不太准确的步骤。
  - 增删：根据实际情况添加漏掉的环节或删除多余的步骤。
4. 执行与计划：
  - 用户将步骤与日历的“早/中/晚”时段关联。
  - 在看板中实时勾选进度。
3. 日历模块细节
- 月视图：全局视角，看到所有任务的 DDL 分布。
- 日详情：点击某天，显示三段式列表（早、中、晚），展示该时段计划完成的步骤。
下一步：技术实现方案
为了实现这个应用，我建议采用以下技术方案：
- 项目初始化：使用 web-db-user 模板。
  - 原因：它自带数据库（存储你的任务和步骤）和 AI 集成能力（调用 OpenAI 拆解任务），且支持用户登录，方便你以后在不同设备上查看。
- 数据库设计：
  - Projects 表：存储任务名、DDL、创建时间。
  - Steps 表：存储步骤内容、所属项目 ID、完成状态、计划执行日期、计划时段（早/中/晚）。
- AI 提示词策略：我会预设一段 Prompt，确保 AI 输出的步骤是“简单的、可操作的”，而不是宏大的空话。